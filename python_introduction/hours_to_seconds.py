# Define the number of hours to convert to seconds
# hours stores the time duration in hours that we want to convert
hours = 2

# Convert hours to seconds using the conversion factor
# There are 3600 seconds in one hour (60 minutes × 60 seconds)
# seconds stores the calculated number of seconds
seconds = hours * 3600

# Print the conversion result in the specified format
# This shows both the original hours and the converted seconds
print(f"{hours} hour(s) is {seconds} seconds")